import React from 'react';
import './Toolbar.css';

const toolbar = (props) => (
    <header className="Toolbar">
    <div className="Account">Account</div>
        <div className="Logo">Logo</div>
        <div className="nav">NavigationItem</div>
    </header>
);

export default toolbar;